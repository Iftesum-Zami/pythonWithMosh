def square(number):
    return number * number
#  we use return statement when we want to reuse the value of the function


result = square(3)
print(result)
# print(result = square(3))  # we can use this instead of previous two lines
